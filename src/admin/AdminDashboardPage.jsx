import { useEffect, useState } from 'react'
import { BarChart, Bar, CartesianGrid, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import { BookOpen, BriefcaseBusiness, Eye, FolderKanban, Heart, Mail, MessageSquare, Users } from 'lucide-react'
import { getDashboard } from '../services/adminApi'

export function AdminDashboardPage() { const [data, setData] = useState(null); useEffect(() => { getDashboard().then(setData) }, []); if (!data) return <div className="admin-skeleton-grid">{Array.from({ length: 6 }, (_, i) => <i key={i} />)}</div>; const cards = [['Dự án', data.projects, FolderKanban, '#6366f1'], ['Năng lực kỹ thuật', data.skills || 0, BriefcaseBusiness, '#3b82f6'], ['Bài kiến thức', data.articles, BookOpen, '#06b6d4'], ['Khách truy cập', data.guests, Users, '#8b5cf6'], ['Lượt yêu thích', data.likes, Heart, '#f43f5e'], ['Bình luận chờ duyệt', data.pendingComments, MessageSquare, '#f59e0b'], ['Tin liên hệ', data.contacts, Mail, '#10b981']]; const chart = cards.slice(0, 5).map(([name, value]) => ({ name, value })); return <><div className="admin-heading"><div><span>Tổng quan hệ thống</span><h1>Dashboard</h1></div><small>Dữ liệu cập nhật theo thời gian thực</small></div><div className="stat-grid">{cards.map(([name, value, Icon, color]) => <article key={name}><i style={{ background: `${color}18`, color }}><Icon /></i><div><small>{name}</small><b>{value}</b></div><span>+ active</span></article>)}</div><div className="chart-grid"><article><header><div><span>Thống kê nội dung</span><h2>Tổng quan dữ liệu</h2></div><Eye /></header><ResponsiveContainer width="100%" height={300}><BarChart data={chart}><CartesianGrid strokeDasharray="3 3" vertical={false} /><XAxis dataKey="name" fontSize={11} /><YAxis allowDecimals={false} /><Tooltip /><Bar dataKey="value" fill="#6366f1" radius={[8, 8, 0, 0]} /></BarChart></ResponsiveContainer></article><article><header><div><span>Phân bổ</span><h2>Engagement</h2></div><Heart /></header><ResponsiveContainer width="100%" height={300}><PieChart><Pie data={chart.slice(2)} dataKey="value" nameKey="name" innerRadius={65} outerRadius={100} paddingAngle={4}>{chart.slice(2).map((_, i) => <Cell key={i} fill={['#8b5cf6', '#f43f5e', '#f59e0b'][i]} />)}</Pie><Tooltip /></PieChart></ResponsiveContainer></article></div></> }
